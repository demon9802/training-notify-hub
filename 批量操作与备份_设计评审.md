# 培训通知助手 v10.2 — 批量操作 & 数据备份 设计评审

> 本文档汇总批量操作与数据备份的设计结论，供统一评审。当前**仅设计、未实现**，按你的要求"先理清、不着急部署"。

---

## 一、为什么做

| 模块 | 背景与诉求 |
|---|---|
| 批量操作 | 多班级培训项目、滚动排期长期运营项目：阶段结构高度雷同，逐阶段配通知重复劳动大 |
| 数据备份 | 真正的本地风险是"工具关闭 / 进程被杀 / 文件损坏"把**正在运营和草稿的项目**弄丢；不是全量恢复诉求，而是"别丢当前工作" |

---

## 二、数据层级模型（所有设计的基础）

```
项目 (Project)
 └─ 阶段 (Stage) × 多
     └─ 通知 (Notification) × 多
         └─ 受众配置 (学员 / 管理 / 讲师) × 最多3
                各含：启用开关 + 发送时间(notifyAt) + 文案 + 模板 + 目标群
```

**关键结论**：三个批量功能作用在不同层级，层级决定放置位置。

---

## 三、三个批量功能设计

### ① 批量生成阶段通知
- **作用层级**：阶段层 / **创建**（跨阶段生成新通知）
- **实际影响**：给"勾选的若干阶段"各新建 1 条通知（基于同一模板 / 文案 / 受众 / 时间规则）
- **放置**：项目编辑页 · 阶段列表**顶部**
- **按钮命名**：`批量生成阶段通知`（名称点明是"通知"、避免"批量设置阶段"的歧义）
- **弹窗三步**：
  1. 选来源：复制某阶段已有通知的配置，或选全局模板
  2. 选目标阶段：勾选要套用的阶段
  3. 时间规则：相对阶段开始时间偏移（课前1天 = 阶段开始-1天；无开始日期则退回绝对时间）
- **范围预览**：底部固定显示"将向 N 个阶段各生成 1 条通知（共 N 条）"
- **典型场景**：多班级培训、滚动排期项目

### ② 批量启停
- **作用层级**：通知层 / **调整**（操作已有通知）
- **实际影响**：把"勾选的若干通知"整体启用 / 停用（连带其下全部受众）
- **放置**：推送概览**工具条**
- **按钮命名**：`批量启停`
- **待定粒度**（需你拍板）：以"通知"为最小单位（含全部受众）【推荐】 / 可下钻到单个受众单独开关

### ③ 批量改时间
- **作用层级**：通知层 → 受众层 / **调整**
- **实际影响**：把"勾选的若干通知"的发送时间统一偏移 / 设定（落到各受众的 notifyAt）
- **放置**：推送概览**工具条**
- **按钮命名**：`批量改时间`
- **操作**：选中通知 → 统一偏移 ±X 天/小时，或设统一时刻
- **待定粒度**（需你拍板）：同上

---

## 四、放置推导结论

| 功能 | 操作对象层级 | 动作类型 | 放置位置 |
|---|---|---|---|
| ① 批量生成阶段通知 | 阶段 | 创建 | 项目编辑页 · 阶段列表顶部 |
| ② 批量启停 | 通知(已有) | 调整 | 推送概览工具条 |
| ③ 批量改时间 | 通知(已有) | 调整 | 推送概览工具条 |

- **创建类（①）贴近阶段列表；调整类（②③）贴近通知总览（推送概览）**——分流逻辑清晰，层级一致。
- 每个批量动作内部再让用户**勾选具体范围**（哪些阶段 / 哪些通知 / 是否含全部受众），弹窗底部固定显示"将影响 N 阶段 / M 通知 / K 受众"，彻底消除歧义。
- 你提到的三种范围正好对应：①=一整个项目的所有阶段；②③可选=单一阶段的所有通知；②③最细=一个通知内的全部受众。

---

## 五、数据备份（兜底）设计

### 风险文件排序（本地）
| 文件 | 内容 | 风险 | 是否入快照 |
|---|---|---|---|
| `projects/*.json` | 各项目的阶段/通知/文案（运营人真实沉淀） | **最高·不可替换** | ✅ 重点 |
| `groups.json` | 群 webhook 通讯录 | 中·可重填但繁琐 | ✅ |
| `templates.json` | 文案模板库 | 中·运营人积累 | ✅ |
| `app-settings.json` | 提醒 webhook | 低·单项 | ✅ |
| `server.js` / `v10.html` / `watchdog` / `_boot` | 应用代码 | 低·可恢复 | ❌（靠版本管理） |

### 方案
- **自动本地快照（主）**：每次保存项目时（或定时）把上述数据复制一份带时间戳副本到 `backups/`，保留最近 N 天（默认 30）。**无需运营人任何操作**即防丢失；快照本身即"历史存储"，满足你提到的历史留存诉求。
- **手动导出 / 导入 JSON（辅）**：降级为"换机 / 迁移 / 给别人看"的便携用途，不靠它日常兜底。
- **触发频率**：低频，自动为主、手动为辅。

---

## 六、待你决策项（统一回复即可）

1. **②③ 操作粒度**：以"通知"为单位（含全部受众）【推荐】 / 可下钻到受众单独操作
2. **放置确认**：按层级分流（①阶段列表顶部 + ②③推送概览）【推荐】 / 统一收进项目头"批量操作"菜单
3. **自动快照保留天数**：默认 30 天？（可调）
4. **图片方案 A/B**：待后续提醒确认（已建每 2 周一提醒，不阻塞）

---

## 七、本次明确不做（避免范围蔓延）

- 图片图文混排：等 A/B 方案选定后再做（独立发图已可用）
- 部署 / 多人协作：等专家库（yili-expert-library）迁移离开 Supabase 后评估
- 定时发送补偿 / 补发 / 告警：已决定**不做**，避免重复发送或内容出错风险
