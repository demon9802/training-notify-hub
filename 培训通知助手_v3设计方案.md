# 培训通知助手 v3 设计方案

## 一、产品定位

一个面向培训运营者的**培训项目群通知编排助手**。

核心目标：让运营者只需填写一次项目信息，系统自动生成各阶段通知文案、选择目标群、按时间自动发布到企业微信群，替代人工逐次编辑和发送。

## 二、关键设计原则

1. **影响面最小**：不申请企微管理权限，只使用群机器人/消息推送 Webhook；
2. **运营者友好**：不暴露 Webhook，只让运营者选群名称/类别；
3. **项目隔离**：多个培训项目并行管理，互不干扰；
4. **文案可控**：自动生成 + 在线修改 + 手动修改不被字段变更覆盖；
5. **可扩展**：后续可接入 Dify、图片识别、私有化企微等。

## 三、使用对象与场景

- 培训运营者、HR、项目负责人；
- 适用于单次培训、多阶段培训、周期性培训项目；
- 覆盖学员群、讲师群、汇报群等多类通知对象。

## 四、产品结构

### 4.1 项目层

| 模块 | 内容 |
|---|---|
| 项目选择 | 新建项目、切换项目、删除项目 |
| 项目公用信息 | 培训项目名称、项目周期类型（单次/多阶段）、培训目的 |
| 培训负责人 | 姓名（必填）、部门/模块（非必填）、联系方式（非必填），支持多人 |
| 通知群登记 | 群名称、群类别（学员群/讲师群/汇报群/其他）、Webhook |

### 4.2 通知层

| 字段 | 说明 |
|---|---|
| 培训阶段 | 如：线上测评、线上微课、线下授课、复习实践 |
| 开始日期 | 该阶段开始日期 |
| 结束日期 | 该阶段结束日期 |
| 通知时间 | 实际发送到群的时间 |
| 形式 | 线上 / 线下 |
| 培训链接/地点 | 线上填链接，线下填地点 |
| 截止提醒 | 如：7月8日（周三）下班前 |
| 目标群 | 从顶部已登记群里多选 |
| 学习材料 | 文档/资料链接 |
| 参考文档 | 参考文档链接 |
| 作业提交 | 作业提交链接 |
| 通知内容 | 自动生成，可直接修改 |

### 4.3 发布层

- 保存项目；
- 立即发送测试；
- 查看完整发布计划；
- 服务定时扫描，到点自动发送。

## 五、文案生成规则

文案风格参考《A+经理人》项目：

- 称呼：学员群用“各位同学，大家好！”，讲师群用“各位讲师，大家好！”，汇报群用“各位领导、同事，大家好！”
- 结构：背景/阶段说明 → 时间安排 → 链接/地点 → 培训目的 → 材料/作业/截止 → 负责人联系方式
- 结尾统一为“如有任何问题，可随时联系：XXX”。

针对目标群自动切换：

- 学员群：常规通知，强调学习任务和时间；
- 讲师群：强调授课/辅导准备；
- 汇报群：强调项目同步和进展汇报。

## 六、识别/保存优先级规则

核心问题：字段修改和手动修改文案之间的冲突。

解决方案：

- 每条通知增加 `contentModified` 标记；
- 用户未手动修改文案时，字段变更自动更新文案；
- 用户手动修改文案后，`contentModified = true`，字段变更不再自动覆盖；
- 提示运营者“字段已变更，建议重新生成文案”；
- 点击“重新生成文案”后，按最新字段生成并清除 `contentModified`；
- **最终发送时，始终以通知内容文本框中的内容为准**。

## 七、多项目隔离机制

- 每个项目保存为独立 JSON 文件：`projects/<项目ID>.json`；
- 项目列表动态读取；
- 切换项目时不互相覆盖；
- 每个项目有独立的通知计划；
- 定时扫描时遍历所有项目，分别触发。

## 八、部署方式对比

| 方式 | 优点 | 缺点 | 适用阶段 |
|---|---|---|---|
| 本地运行 | 零成本、零权限、最快验证 | 需要电脑长期开机 | 个人测试/小范围 |
| EdgeOne Makers | 已连接、可快速部署 | 默认链接有时效、固定域名需备案 | 小团队共享 |
| 公司内网服务器 | 稳定、域名可控、长期运行 | 需要服务器资源 | 正式上线 |
| Dify | 自然语言入口、AI 文案 | 定时能力受限、多项目管理弱 | 辅助入口 |

## 九、企微/私有化企微说明

### 普通企业微信

- 使用群机器人/消息推送 Webhook；
- 接口地址：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...`；
- 主要支持内部群，外部客户群需单独评估。

### 私有化企业微信

- 调用方式完全一样（HTTP POST + JSON）；
- 只是 API 域名不同，通常是内网域名；
- 需要在配置里替换为私有化企微的 endpoint；
- 其他逻辑无需修改。

### 是否有绕开企微差异的方式？

**没有完全绕开的方式。** 群通知最终必须发到某个即时通讯工具。如果不走企微，就要换其他工具（如钉钉、飞书、邮件、短信），但你们场景是企业微信内部使用。所以最稳的做法是：代码层保持通道抽象，企微和私有化企微只是 endpoint 不同，切换配置即可。

## 十、Dify 集成建议

Dify 适合作为：

1. **自然语言创建入口**：运营人用对话方式描述培训安排，Dify 调用本助手 API 创建项目；
2. **文案生成增强**：Dify 根据项目信息生成更自然的通知文案；
3. **简单定时发送**：Dify Workflow + 定时触发器调用 Webhook。

但不建议完全用 Dify 替代本助手，因为：

- Dify 不擅长多项目持久化管理；
- Dify 不擅长复杂多阶段通知编排；
- Dify 的定时能力可能受限；
- 多人协作和版本管理较弱。

推荐架构：

```text
运营人
  ├── 本助手 Web 页面（主要入口）
  └── Dify 对话入口（辅助入口）
        ↓
  本助手后端服务
        ↓
  企业微信群机器人 Webhook
```

## 十一、图片识别规划

### 当前

支持文字/表格粘贴识别。

### 后续

可接入：

- 腾讯云通用文字识别（OCR）；
- 腾讯文档图片识别；
- 多模态大模型视觉理解。

### 成本

- OCR 按调用量计费，单次识别成本较低；
- 多模态模型成本相对较高；
- 建议先验证文字识别是否够用，再评估图片识别 ROI。

## 十二、文档与链接发送

- 培训链接、学习材料、参考文档、作业提交链接，最终都以 Markdown 链接形式发送到企微群；
- 支持普通网页链接、腾讯文档/表格、微云链接等；
- 线下文档需要先上传到可访问的云端地址（如腾讯文档、微云），再把链接填入对应字段；
- 空链接不会出现在最终通知中。

## 十三、后续迭代方向

1. 接入 Dify 作为自然语言创建入口；
2. 接入图片识别，支持日程图自动解析；
3. 支持私有化企微 endpoint 配置；
4. 增加发送日志、失败重试、已读统计；
5. 增加项目模板，快速创建同类培训；
6. 增加权限管理，区分管理员和运营者。
