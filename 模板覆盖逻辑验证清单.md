# 培训通知助手 v10 模板覆盖逻辑 · 逐项验证清单

> 验证对象：`training-notification-v10.html`（前端，需经本地服务 `http://localhost:8788/v10` 打开，方能触发保存/发送）
> 目标：确认「受众文案 / 模板 / 节点」三者之间**不存在误覆盖**。
> 每项先按「操作」手动验证，再对照「预期结果」打勾；异常请截图编号回传。

---

## 0. 前置准备（每次验证前必做）
1. 打开一个**测试项目**（不要动生产项目数据）。
2. 在「文案模板」管理里准备：模板 A（节点=启动通知，群=学员）、模板 B（节点=中程提醒，群=学员），内容明显不同。
3. 在该项目里建一个通知 N，默认节点=启动通知。
4. 准备一个**测试群 webhook**（隔离于生产群），填入受众目标群。

---

## 1. 手动改文案 / 插字段，不会反向改模板
**操作**
- 在 N 的「学员」受众下，点「选择模板」→ 应用模板 A。
- 在文案框手动改写几处文字（如把标题改掉）。
- 点「插入字段 ▼」→ 选 `{{项目名}}` 等变量插入。
**预期结果**
- 学员版文案出现你的改写 + 变量占位符。
- 回到「文案模板」管理，模板 A 的原文**一字未变**。
- 切换其他受众（讲师/经理）或新建通知应用模板 A，内容仍是模板 A 原文。
✅ 通过 ✅ （用户实测 2026-08-13 确认）

> 静态确认（代码层）：文案框 `input` 与字段插入只写入 `n.audienceContent[aud].content`，模板存储 `TEMPLATES` 全程不被触碰 → 设计上不会反向改模板。**实测验证通过。**

---

## 2. 「应用模板」只纯重置当前受众版本
**操作**
- 在 N 的「学员」受众下，先手动改写文案（使其非空）。
- 点「选择模板」→ 选模板 B（注意模板 B 节点=中程提醒）。
- 弹出确认「应用模板会覆盖当前文案」→ 确认。
**预期结果**
- 仅「学员」这一受众版本被重置为模板 B 内容；「讲师/经理」版本内容**不受影响**。
- 学员版 `templateId` 指向 B、`templateAppliedAt` 刷新。
- ⚠️ 注意副作用：应用后 **通知 N 的节点会被改成模板 B 的节点（中程提醒）**（代码 `n.node = t.node`）。请确认这是你期望的行为——它改的是通知级节点，不是模板。
- 模板 B 本身内容不变。
✅ 通过 ✅ （用户实测 2026-08-13 确认：不会更改其他受众）

---

## 3. 切换通知节点仅填充空白受众版本
**操作**
- 找一个已有手动定制文案的受众（如学员版已写好专属内容）。
- 将该通知切换到不同节点（如从中程提醒切到启动通知）。
**预期结果**
- 期望：仅**空白**受众版本被新节点对应模板自动填充；**已手写/已定制**的受众内容**保留不丢**。
- 请重点确认：切换节点后，你已定制的文案有没有被悄悄覆盖。

⚠️ **静态审查重要发现（需你实测确认）**
- 代码中 `applyNodeTemplates(n, onlyEmpty)` 函数确实实现了「只填空白受众」逻辑（`if(onlyEmpty && ac.content.trim()) return;`），但**全文件无任何调用点**——当前 UI 没有独立的「切换通知节点」控件会触发它。
- 通知的节点 `n.node` 目前只在「应用模板」（见第 2 项）和「复制通知/复制模板」时被赋值，**没有单独切换节点并自动填充受众的交互入口**。
- 结论倾向：**第 3 项描述的「切节点自动填空白受众」行为当前并未接通**。请实测确认：
  - 若你本就预期该入口存在 → 这是功能缺口，需补接 `applyNodeTemplates(n, true)`；
  - 若你接受「切节点=通过应用模板间接实现」→ 则以第 2 项为准，第 3 项可视为不适用。
✅ 功能未接通，按上说明处理 ✅ （用户实测 2026-08-13 确认：通知本身不单独选择"通知节点"，节点是模板的属性而非通知的独立控件。此场景不适用。）

---

## 4. 模板更新后，旧配置通知显示黄字提醒，由运营人自决
**操作**
- 在 N「学员」受众应用模板 A（记录此时 `templateAppliedAt`）。
- 去「文案模板」管理，编辑模板 A 内容并保存（此时 `updatedAt` 刷新）。
- 回到 N 的「学员」受众编辑区。
**预期结果**
- 该受众区出现**黄底警示框**：「该文案使用的模板「A」已更新，是否使用最新版本？」+ 两个按钮：**保留当前文案** / **使用新版**。
- 点「保留当前文案」：警示消失，学员版**维持你现有文案**，`templateAppliedAt` 刷新为现在（之后模板再改才会重新提醒）。
- 点「使用新版」：学员版内容被重置为模板 A 新内容，警示消失。
- 讲师/经理等**未用模板 A** 的受众**不弹**此提醒（正确）。
- ⚠️ 注意：在「直接粘贴」模式下该黄字提醒被隐藏（`data-template-only`），符合预期。
✅ 通过 ✅ （用户实测 2026-08-13 确认，截图已提供：黄字提醒「该文案使用的模板「测试T-A」已更新，是否使用最新版本？」+「保留当前文案」/「使用新版」按钮正常显示）

> 静态确认（代码层）：`isTemplateOutdated(ac)` 比较 `模板.updatedAt > 受众.templateAppliedAt`，命中则渲染黄字框；`keepOldTpl` 仅刷新时间戳、`useNewTpl` 调 `applyTemplateToAudience` 重置内容，二者都只作用于当前受众，互不串扰。

---

## 汇总结论（由执行人填写）
| 项 | 结论 | 备注 |
|---|---|---|
| 1 改文案/插字段不反向改模板 | ✅ 通过 | 手动修改/插变量均不触碰模板存储 |
| 2 应用模板只重置当前受众 | ✅ 通过 | 仅当前受众被重置，其他受众不受影响 |
| 3 切节点仅填空白受众 | ⚠️ 不适用 | 通知无独立"节点选择器"，节点是模板属性。`applyNodeTemplates(n,true)` 函数存在但未接 UI，当前无需此功能 |
| 4 模板更新黄字提醒+自决 | ✅ 通过 | 黄字提醒正确显示，保留/使用新版二选一正常工作 |

**最终结论**：**3/4 项通过，1 项不适用（非 bug，是产品逻辑确认：节点归属模板而非通知）。不存在误覆盖风险。** ✅

> 测试执行人：用户 | 测试时间：2026-08-13 11:20 | 测试环境：localhost:8788/training-notification-v10.html（独立本地服务，v10-data/ 隔离存储）
