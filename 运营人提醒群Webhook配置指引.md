# 运营人提醒群 Webhook 配置指引

> 状态：**未配置**（截至 2026-08-24，`app-settings.json` 中 `reminderWebhook` 为空）
> 影响：培训通知助手的「节点提醒」（提前 1 天确认、发送前 2 小时测试）目前**仅页面「推送概览 → 临近节点」看板生效，不会推送企业微信**。

## 配置步骤（约 2 分钟）

1. **在企业微信建一个小群**：仅拉「你」和「领导」两人（不要拉其他人，避免节点提醒打扰团队）。
2. **添加群机器人**：进群 → 右上角 `…` →「群机器人」→「添加机器人」→ 取名（如「节点提醒」）→ 创建。
3. **复制 Webhook 地址**：机器人创建后会给出形如
   `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxx`
   的地址，整段复制。
4. **填入通知助手**：打开 `http://localhost:8788/v10` → 左侧「群通讯录」弹窗顶部「运营人提醒群」卡片 → 把地址粘进「提醒群 Webhook URL」输入框 → 点「保存」。
5. **确认生效**：保存后该卡片状态应变为「当前已配置提醒通道，到点将推送企微消息。」

## 完成后

配好后告诉我一声「已配置」，我会停止每周一的这条提醒。

## 说明

- 该 webhook 仅用于**运营人自己的节点确认/测试版提醒**，与正式发给学员/讲师群的通知走的是不同通道，互不影响。
- 留空则只走页面看板；填了即启用企微推送。
