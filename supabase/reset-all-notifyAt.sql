-- ============================================================
-- 培训通知助手 v10.7.8 重置脚本（配合 __reset_all_notifyAt.js）
-- ============================================================
-- 用途：清空 tn_sends 表所有历史脏记录，让用户重新逐个设置时间后定时发送链路能干净触发
-- 适用：之前用"立即发送测试"或历史 scheduler 留下的 status=sent 脏记录
-- 频率：每次大规模测试前 / 怀疑有死循环残留时跑一次
-- 安全：只动 tn_sends 表；tn_kv 项目数据完全不动
-- ============================================================
-- 操作：Supabase 控制台 → SQL Editor → New query → 粘贴以下 → Run

DELETE FROM tn_sends;

-- 验证是否清空：
SELECT count(*) AS remaining_rows FROM tn_sends;

-- ============================================================
-- 后续步骤：
-- 1) cd 到项目根目录跑 node __reset_all_notifyAt.js（清 tn_kv 里的 sentAt/sentAudiences/notifyAt/reminder*）
-- 2) 回到 EdgeOne 页面 → 通知列表 → 逐个修改发送时间（确保至少比当前时间晚 1 分钟）
-- 3) 客户端 60s 心跳 或 pg_cron 5min 自动触发
-- ============================================================