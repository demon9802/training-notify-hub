# ============================================================
# 培训通知助手 — 本地环境变量模板（复制为 .env 后填写）
# 重要：.env 仅存在于你这台电脑，含密钥，切勿提交到 Git / 分享给他人。
# 也不要在 .env 里填入「其他项目」的 Supabase / Netlify 凭据，本工具只用自己新建的项目。
# ============================================================

# 在 Supabase 新建一个【独立项目】，不要复用其他项目。
# Project Settings → API 中获取：
TN_SUPABASE_URL=
TN_SUPABASE_SERVICE_ROLE_KEY=

# 可选：若不用 service_role，也可填 anon key（需自行配 RLS）
# TN_SUPABASE_ANON_KEY=

# 数据后端：local（默认，纯本地）| hybrid（本地 + Supabase 镜像）
TN_DATA_BACKEND=hybrid

# 共享访问密码（轻量登录）：上线 Netlify 多人使用时必填，未填则本地调试开放访问。
# 你和领导共用同一个密码即可，登录后各浏览器独立保存 token，互不顶号。
TN_APP_PASSWORD=dacc2026

# 可选：token 派生盐值（不填则用默认值，重启不失效）
# TN_AUTH_SECRET=
