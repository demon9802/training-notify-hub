# 企微通知与日程提醒助手：汇报摘要

## 项目定位

建设一个基于企业微信的培训、会议、活动通知与日程提醒助手，用于替代人工在各时间节点发群通知、建日程、点对点提醒、催确认、催签到和补通知。

## 核心价值

- 减少人工反复发通知和催办；
- 避免重要会议、培训、活动漏提醒；
- 群通知、个人提醒、日程提醒统一管理；
- 对未确认、未签到、未完成任务人员自动补推；
- 保留发送日志和完成状态，便于复盘。

## 核心功能

1. 活动 / 培训 / 会议创建  
   配置名称、时间、地点、参与人、群、负责人、材料链接等。

2. 多节点提醒  
   支持开始前 1 天、1 小时、15 分钟、开始时、结束后等自动提醒。

3. 群通知  
   通过企业微信群机器人发送公告、提醒、变更、签到、总结等消息。

4. 个人提醒  
   通过企业微信自建应用向指定成员发送个人消息，适合未确认、未签到、未完成任务补推。

5. 企业微信日程  
   自动创建会议 / 培训日程，写入成员日历并触发企微自带提醒。

6. 状态记录  
   记录消息是否发送成功、谁已确认、谁已签到、谁未完成。

## 实现阶段

### 阶段一：轻量 MVP

先实现活动创建、提醒模板、多节点定时提醒、群机器人通知和企微日程。

效果：快速替代人工群提醒，减少漏发和忘发。

### 阶段二：个人提醒闭环

接入企业微信自建应用，支持个人精准提醒、确认参加、签到、未完成自动补推和状态看板。

效果：从“通知发出去”升级到“知道谁没完成，并自动催办”。

### 阶段三：智能化增强

加入 Dify / AI 能力，支持自动生成通知文案、推荐提醒节奏、自然语言创建活动、自动生成复盘。

效果：降低运营配置成本，提高复用和规模化能力。

## 是否必须部署后端？

如果只做简单群通知，可以不做完整后端，用群机器人 + Dify / 表格 / 自动化工具先跑 demo。

但如果要实现完整效果，例如个人提醒、日程、签到确认、状态看板、失败重试、自动补推，就需要一个轻量后端或自动化服务。

这个后端不是“写入企业微信”，而是在外部保存活动和规则，到点后调用企业微信开放接口，把消息、日程、提醒发送到企微。

## Dify 是否可行？

Dify 可行，但更适合做原型和 AI 辅助，不建议单独承担完整系统。

Dify 适合：

- 快速做 demo；
- 定时触发简单群通知；
- 自动生成通知文案；
- 根据活动信息生成提醒计划；
- 做运营助手入口。

Dify 不太适合单独承担：

- 大量活动的多节点动态调度；
- 企业微信成员、群、日程的复杂映射；
- 严格失败重试；
- 完整签到、确认和状态看板。

## 推荐方案

推荐采用“轻量后端 + 企业微信自建应用 + 群机器人 + Dify 辅助”的方式。

- 轻量后端负责可靠提醒、数据、调度、日志和状态；
- 企业微信群机器人负责群通知；
- 企业微信自建应用负责个人提醒和日程接口；
- Dify 负责 AI 文案生成、提醒计划生成和低代码流程辅助。

## 推荐落地路径

1. 先用群机器人 + Dify / 脚本做一个 demo；
2. 再接企业微信自建应用，实现个人提醒和日程；
3. 然后补轻量后台，实现配置、日志、签到和补推；
4. 最后加入 AI 文案、自然语言创建活动和复盘能力。

## 一句话结论

如果只是验证需求，可以先用 Dify + 群机器人快速试点；如果要真正替代人工并形成稳定闭环，建议建设轻量后端，并用 Dify 做 AI 和低代码辅助。
