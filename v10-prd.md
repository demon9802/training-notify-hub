# 培训通知助手 v10.1 PRD

## 1. 目标
把当前"项目→通知"的扁平模型，升级为"项目→阶段→任务→通知"的分层模型，减少重复配置，支持图文混排发送，并提供不依赖建群的运营人确认机制。

## 2. 数据模型

### 2.1 项目（Project）
- id, name, description
- 阶段列表 stages[]
- 群通讯录 groups[]
- 运营人设置（可选）

### 2.2 阶段（Stage）
- id, name
- startDate, endDate（阶段整体时间窗口）
- 学习任务列表 tasks[]

### 2.3 学习任务（Task）
- id, name, description
- dueDate（截止日期，与阶段起止日期独立）
- 附件 attachments[]（链接/图片）

### 2.4 通知（Notification）
- 关联到阶段 stageId
- 包含任务 taskIds[]（可多选，支持单个任务或阶段内多个任务合并发送）
- node：start / midway / due / post-due（沟通节点：启动/中程提醒/截止提醒/截止后通晒）
- audienceContent：{ student, lecturer, manager }，**一套通知内为三个受众各维护一套独立文案**，可单独开关、独立编辑
- targetGroups：按受众分别指定发往哪些群（如学员版→学员群，讲师版→讲师群，管理版→管理群）
- 发送状态/status、发送时间/sentAt

## 3. 关键功能

### 3.1 阶段与任务
- 一个项目可添加多个阶段
- 一个阶段可添加多个学习任务
- 每个任务独立设置截止日期、内容、附件

### 3.2 通知与任务的关系
- 一个阶段下可创建多条通知
- 每条通知可选择包含该阶段下的 1-N 个任务
- 支持两种典型场景：
  - **单任务通知**：一个任务对应一条通知（连续不交叉的学习任务）
  - **多任务合并通知**：一个阶段内多项任务合并到一条通知发送

### 3.3 多受众通知（一套通知，多套版本）
- **一条通知内**为学员/讲师/管理群分别维护独立文案版本，而非建多条通知
- 每个版本可单独开关、独立微调：
  - **学员版**：详细，含操作步骤、截止日、附件
  - **讲师版**：中等，强调配合环节
  - **管理群版**：极简，项目进展 + 关键数据
- 发送时按"目标群 → 受众"映射，自动选对应版本文案发往对应群
- 提供"复制此通知并修改"作为兜底（极少数异构场景才需要）

### 3.4 图文混排发送
- 采用企微群机器人 **markdown_v2** 消息类型
- 支持在单条消息中使用 `![图片说明](公网图片URL)` 语法插入图片
- 图片可自然穿插在文字段落之间，实现单条消息图文混排
- 图片来源两种，运营人**无需单独"另存→浏览→上传"步骤**：
  - **粘贴上传**：在编辑器中 Ctrl+V 直接粘贴截图，系统自动上传到已配置的公网对象存储并返回 URL 插入
  - **直接填 URL**：若图片已托管（如在线文档提供的直链），可直接粘贴 URL
- 内容更新时重新粘贴截图即替换原 URL，不产生"重新上传"的额外负担
- 链接使用 `[标题](URL)` 语法，可自定义显示文字

### 3.5 附件链接显示文字
- 附件增加"链接标题"字段 title
- 渲染为 `[标题](URL)`，不再统一显示"链接"
- 未填写标题时默认使用附件标签

### 3.6 运营人确认（不强制建群）
- **发送前预览**：系统内弹窗模拟企微消息效果
- **可选试发送**：可指定任意已有群 webhook，发送时自动加【测试】前缀
- **阶段切换预警**：登录系统时首页提示即将进入下一阶段，需确认通知
- **今日待发送清单**：首页显示当天将要发送的通知列表

## 4. 页面结构

```
首页
├── 项目列表
└── 今日待发送

项目编辑页
├── 基本信息
├── 群通讯录
├── 阶段列表
│   └── 阶段卡片
│       ├── 阶段起止日期
│       ├── 学习任务列表
│       │   └── 任务卡片（纯学习任务，不含通知）
│       │       ├── 截止日期（每个任务独立）
│       │       ├── 内容
│       │       └── 附件
│       └── 通知列表（独立于任务，可引用 1-N 个任务）
│           └── 通知卡片
│               ├── 关联任务（多选：1 个或多个任务合并发送）
│               ├── 沟通节点（启动/中程/截止/截止后）
│               ├── 受众版本（学员/讲师/管理群 各一套独立文案，可独立开关编辑）
│               └── 目标群（按受众分别指定发往哪些群）
└── 发送记录
```

## 5. 群类型名称
- 学员群
- 讲师群
- 管理群（原"汇报群"）

## 6. 非目标（v10.1 不做）
- 不实现根据名单自动生图（保留后续可能）
- 不上云，继续本地服务
- 不做复杂权限/登录
- 不切换企微应用 API，继续用群机器人 webhook（markdown_v2 已满足图文混排）

## 7. 技术方案
- 前端：继续基于 training-notification-demo.html 扩展
- 后端：继续基于 training-notification-server.js 扩展
- 消息类型：采用企微群机器人 **markdown_v2**，支持图文混排与自定义链接文字
- 图片存储：编辑器支持 Ctrl+V 粘贴自动上传；后端将图片上传至"已配置的公网对象存储"并返回 HTTPS URL 供 markdown_v2 使用。推荐 **腾讯云 COS**（一次性配置，运营人零额外操作，费用按量约几毛/月）；非公开云盘（腾讯文档/微云）无法提供可直接嵌入的公开图片 URL，不作首选。若后续整体部署上云，也可直接使用服务端 `uploads/` 目录对外提供 URL。
- 定时任务：保留现有每分钟扫描逻辑

## 8. 验收标准
- [ ] 可创建阶段和任务，任务有独立截止日期
- [ ] 一条通知可选择包含阶段内 1-N 个任务
- [ ] 一个任务/阶段可为学员/讲师/管理群生成不同文案
- [ ] 通知使用 markdown_v2 发送，单条消息内可实现文字+图片混排
- [ ] 附件链接可自定义显示文字
- [ ] 发送前可在系统内预览效果
- [ ] "汇报群"已统一改为"管理群"
