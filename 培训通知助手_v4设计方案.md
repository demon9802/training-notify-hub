# 培训通知助手 v4 设计方案

## 一、产品定位

面向培训运营者的**培训项目群通知编排助手**。

核心目标：让运营者只需填写一次项目信息，系统自动生成各阶段通知文案、选择目标群、按时间自动发布到企业微信群，替代人工逐次编辑和发送。

## 二、关键设计原则

1. **影响面最小**：不申请企微管理权限，只使用群机器人/消息推送 Webhook；
2. **运营者友好**：不暴露 Webhook，只让运营者选群名称/类别；
3. **项目隔离**：多个培训项目并行管理，互不干扰；
4. **文案可控**：自动生成 + 在线修改 + 字段变更智能局部同步；
5. **信息结构合理**：培训目的放开头、截止提醒与日期融合、附件资源动态添加；
6. **可扩展**：后续可接入 Dify、图片识别、私有化企微等。

## 三、使用对象与场景

- 培训运营者、HR、项目负责人；
- 适用于单次培训、多阶段培训、周期性培训项目；
- 覆盖学员群、讲师群、汇报群等多类通知对象。

## 四、产品结构

### 4.1 项目层

| 模块 | 内容 |
|---|---|
| 左侧项目列表 | 显示所有项目，支持切换、复制、删除 |
| 项目公用信息 | 培训项目名称、项目周期类型（单次/多阶段）、培训目的 |
| 培训负责人 | 姓名（必填）、部门/模块（非必填）、联系方式（非必填），支持多人 |
| 通知群登记 | 群名称、群类别（学员群/讲师群/汇报群/其他）、Webhook |
| 智能初始化 | 粘贴日程文字自动识别，只影响当前项目 |

### 4.2 通知层

| 字段 | 说明 |
|---|---|
| 培训阶段 | 如：线上测评、线上微课、线下授课、复习实践 |
| 开始日期 | 该阶段开始日期 |
| 结束日期 | 该阶段结束日期 |
| 通知时间 | 实际发送到群的时间 |
| 形式 | 线上 / 线下 |
| 培训链接/地点 | 线上填链接，线下填地点 |
| 截止提醒 | 同结束日期 / 自定义 |
| 目标群 | 从顶部已登记群里多选 |
| 附件资源 | 动态添加：学习材料、参考文档、作业提交、日程安排、分组信息、其他资料 |
| 图片 | 可选，支持上传 |
| 通知内容 | 自动生成，可直接修改 |

### 4.3 发布层

- 保存项目；
- 立即发送单条/全部；
- 查看完整发布计划（弹窗）；
- 服务定时扫描，到点自动发送。

## 五、文案生成规则

### 结构

```text
称呼

培训目的（项目公用信息）

阶段说明 + 时间安排
地点/链接
附件资源
截止提醒

联系方式
```

### 称呼

- 学员群：各位同学，大家好！
- 讲师群：各位讲师，大家好！
- 汇报群：各位领导、同事，大家好！

### 标题

- 不再生成 `【XX通知】` 前缀；
- 直接以称呼开头。

## 六、字段变更与手动修改的优先级

### 核心逻辑

- 每条通知保存 `fieldSnapshot`（字段快照）；
- 用户未手动修改文案时，字段变更自动重新生成；
- 用户手动修改文案后，字段变更尝试**局部替换**：
  - 阶段名称、日期范围、地点/链接、截止时间、附件、联系人等；
  - 替换成功：静默更新；
  - 替换失败：提示“字段已变更，建议检查或重新生成”。

### 重新生成

- 点击“重新生成文案”会按最新字段完整生成；
- 会清除 `contentModified` 标记。

## 七、项目隔离机制

- 每个项目保存为独立 JSON 文件：`projects/<项目ID>.json`；
- 左侧项目列表动态读取；
- 切换项目时不互相覆盖；
- 每个项目有独立的通知计划；
- 定时扫描时遍历所有项目，分别触发。

## 八、部署方式对比

| 方式 | 优点 | 缺点 | 适用阶段 |
|---|---|---|---|
| 本地运行 | 零成本、零权限、最快验证 | 需要电脑长期开机 | 个人测试/小范围 |
| EdgeOne Makers | 已连接、可快速部署 | 默认链接有时效、固定域名需备案 | 小团队共享 |
| 公司内网服务器 | 稳定、域名可控、长期运行 | 需要服务器资源 | 正式上线 |
| Dify | 自然语言入口、AI 文案 | 定时能力受限、多项目管理弱 | 辅助入口 |

## 九、企微/私有化企微说明

### 普通企业微信

- 使用群机器人/消息推送 Webhook；
- 接口地址：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...`；
- 主要支持内部群，外部客户群需单独评估。

### 私有化企业微信

- 调用方式完全一样（HTTP POST + JSON）；
- 只是 API 域名不同，通常是内网域名；
- 需要在配置里替换为私有化企微的 endpoint；
- 其他逻辑无需修改。

## 十、图片发送说明

- 支持在每条通知中上传图片；
- 发送时：
  1. 先调用 `/api/send` 发送文本；
  2. 再调用 `/api/send-image` 上传图片到企微获取 media_id；
  3. 再发送图片消息。
- 企业微信图片要求：2MB 以内，JPG/PNG。

## 十一、Dify 集成建议

Dify 适合作为：

1. **自然语言创建入口**：运营人用对话方式描述培训安排，Dify 调用本助手 API 创建项目；
2. **文案生成增强**：Dify 根据项目信息生成更自然的通知文案；
3. **简单定时发送**：Dify Workflow + 定时触发器调用 Webhook。

推荐架构：

```text
运营人
  ├── 本助手 Web 页面（主要入口）
  └── Dify 对话入口（辅助入口）
        ↓
  本助手后端服务
        ↓
  企业微信群机器人 Webhook
```

## 十二、后续迭代方向

1. 接入 Dify 作为自然语言创建入口；
2. 接入图片识别，支持日程图自动解析；
3. 支持私有化企微 endpoint 配置；
4. 增加发送日志、失败重试、已读统计；
5. 增加项目模板，快速创建同类培训；
6. 增加权限管理，区分管理员和运营者。
