# 培训通知助手：定时触发与部署方案说明

## 一、为什么定时触发必须有一个持续运行的后端？

当前 Demo 的工作模式：

1. 你在浏览器里编辑项目、设置通知时间。
2. 前端把项目数据发给本地 Node 服务保存到 `projects/*.json`。
3. Node 服务每分钟扫描一次文件，发现到点且开启“自动发送”的通知，就调用企业微信群机器人 Webhook 发出去。

**关键点**：第 3 步是一个“后台常驻进程”。如果电脑关机、Node 服务被关闭、网络断开，扫描就停了，到点的通知就不会被补发（除非服务重新启动后在 24 小时补发窗口内 catching up）。

所以“定时自动发”和“后端持续在线”是绑定的。这不是功能复杂度问题，而是基础运行条件问题。

---

## 二、本地 Demo 的适用边界

本地 Demo（`node training-notification-server.js`）最适合：

- 验证文案效果、群机器人连通性。
- 临时手动发送“立即发送全部”。
- 小范围、短周期测试（几小时到一两天，电脑保持开机）。

**不适合**：

- 长期依赖定时自动发（电脑不可能一直开机）。
- 多人共用（项目存在本地文件，服务只在你的电脑上）。
- 正式培训运营（一旦漏发影响较大）。

---

## 三、可选部署方案（按推荐程度排序）

### 方案 A：EdgeOne Makers / EdgeOne Pages + 边缘函数（推荐优先尝试）

**适用场景**：想把前端页面放到线上，同时用边缘函数处理定时发送。

- **前端**：`training-notification-demo.html` 是纯静态页面，可以直接部署到 EdgeOne Pages（你当前已连接 EdgeOne Makers）。
- **后端定时**：EdgeOne Makers 支持边缘函数（Edge Functions），可以写一段 JS 定时任务，读取存储的项目数据并调用 Webhook。具体能力（能否常驻定时、能否写文件/数据库）需要看 EdgeOne Makers 当前版本。
- **优点**：
  - 和公司现有连接（EdgeOne Makers）一致，部署门槛低。
  - 静态页面访问快，无需备案即可内部使用。
- **风险/待确认**：
  - 边缘函数是否支持“每分钟触发一次”的定时任务？
  - 项目数据存在哪里？（边缘 KV / 数据库 / 对象存储？）
  - Webhook 调用是否受边缘函数出网限制？

**结论**：这是一个值得先验证的方向，但需要你或运维确认 EdgeOne Makers 边缘函数的具体能力。如果它能跑定时任务 + 持久化存储，这就是成本最低、最干净的方案。

---

### 方案 B：腾讯云 SCF（云函数）+ 定时触发 + EdgeOne Pages 前端

**适用场景**：不想买服务器，只愿意为零星调用付费。

- **前端**：部署到 EdgeOne Pages（免费/低成本静态托管）。
- **后端**：把发送逻辑拆成一个腾讯云 SCF 函数，用“定时触发器”每分钟执行一次。
- **存储**：项目数据放到腾讯云数据库（如 TDSQL-C Serverless / MySQL / Redis）或对象存储 COS 中，SCF 读取后发送。
- **费用**：
  - 云函数按调用次数和运行时长计费，一分钟触发一次，一个月约 4.3 万次调用，免费额度通常够用。
  - 数据库存储少量项目数据，费用很低。
- **优点**：
  - 无需服务器长期开机。
  - 定时触发由平台保证，比自维护更稳。
  - 前端和后端完全分离，便于多人使用。
- **缺点**：
  - 需要一点云资源配置（函数、触发器、数据库/COS）。
  - 调试云函数比本地慢。

**结论**：这是目前“不依赖公司服务器、个人成本最低、定时触发最稳”的最优解。

---

### 方案 C：腾讯云轻量应用服务器 / CVM

**适用场景**：希望一次性搞定，不想研究云函数和触发器。

- 购买一台最便宜的轻量服务器（约 30–60 元/月）。
- 把 `training-notification-server.js` 部署上去，用 `pm2` 或 `systemd` 保活。
- 前端页面可以仍用 EdgeOne Pages，也可以直接通过服务器 IP+端口访问（内部使用免备案）。
- **优点**：
  - 和本地 Demo 几乎一致，迁移成本最低。
  - 可以跑完整 Node 服务、写本地文件、定时扫描。
- **缺点**：
  - 需要持续付费。
  - 个人服务器需要你自己维护安全、备份、续费。

**结论**：如果个人愿意承担一年几百元成本，这是最省心的方案。

---

### 方案 D：公司内网常开机器 / NAS / 旧笔记本

**适用场景**：公司内部有一台 24 小时开机的电脑或 NAS。

- 在这台机器上安装 Node.js，用 `pm2` 启动服务。
- 设置开机自启，保证重启后自动恢复。
- 如果机器在内网，需要配合内网穿透或 VPN 才能从外部编辑页面。
- **优点**：
  - 无额外云成本。
  - 数据留在公司内部。
- **缺点**：
  - 需要 IT 或运维支持。
  - 机器断电/断网会中断定时。
  - 多人访问需要解决内网可达性。

**结论**：适合公司已有稳定内网环境，且你能申请到一台常开机器的情况。

---

### 方案 E：本地电脑 + 开机自启动（仅测试）

- 把 Node 服务加入 Windows 开机启动或任务计划程序。
- 保持电脑不关机、不睡眠。
- **结论**：只适用于临时演示，不能作为正式方案。

---

## 四、推荐组合

| 阶段 | 前端 | 后端/定时 | 成本 | 稳定性 |
|---|---|---|---|---|
| 现在验证 | 本地 `localhost:8788` | 本地 Node | 0 | 低（依赖本机） |
| 小范围正式试用 | EdgeOne Pages | 腾讯云 SCF 定时触发 + COS/数据库 | 接近 0 | 高 |
| 长期正式使用 | EdgeOne Pages 或公司内网 | 轻量服务器 / 公司内网服务器 | 中 | 高 |
| 零服务器尝试 | EdgeOne Pages | EdgeOne Makers 边缘函数（需验证定时能力） | 0 | 待验证 |

**当前最建议的下一步**：

1. 先在本地把 v6 的功能跑通（新增按钮位置、图片、链接、排序、定时加固）。
2. 然后尝试把前端部署到 EdgeOne Pages，后端用腾讯云 SCF 定时触发做最小可行验证（只保留“读取项目 + 到点发送”两个接口）。
3. 如果 SCF 验证成功，就完全替代本地服务；如果不愿用云函数，再考虑轻量服务器。

---

## 五、正式部署前还需要补齐的安全与设计点

1. **Webhook Key 不能放在前端**：当前 Demo 的 Webhook 地址存在前端 state 里。正式部署时，Webhook key 必须只保存在后端环境变量或加密存储中，前端只保存“群 ID”，发送时由后端根据 ID 取对应 Webhook。
2. **项目数据持久化**：从本地 JSON 文件迁移到数据库或 COS，支持多用户、备份。
3. **发送记录与重试**：已发送、失败、跳过的记录需要可视化，失败支持手动重试。
4. **权限隔离**：不同运营人只能看到自己的项目，或按角色共享。
5. **Webhook 限流**：企微机器人有频率限制（如 20 条/秒），大量群同时发送时需要排队。

---

## 六、一句话总结

> 定时自动发必须依赖一个持续运行的后端。本地 Demo 只能验证和手动发；要想真正“到点自动发”，下一步建议用 **EdgeOne Pages 托管前端 + 腾讯云 SCF 定时触发** 做零服务器验证，验证通过后再决定长期使用 SCF 还是购买轻量服务器。
