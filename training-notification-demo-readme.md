# 培训通知助手 v4 - 使用说明

## 启动方式

```bash
node training-notification-server.js
```

打开浏览器访问：

```text
http://localhost:8788
```

## v4 主要更新

### 1. 左侧项目列表

- 所有培训项目以卡片形式展示在左侧；
- 显示项目名称、周期类型、最近更新时间；
- 点击即可切换项目，互不干扰；
- 支持复制项目、删除项目。

### 2. 通知条目导航

- 多阶段培训的通知以左侧导航 + 右侧编辑区形式呈现；
- 当前编辑的通知展开，其他通知折叠，减少页面滚动；
- 点击导航或“展开编辑”可快速跳转。

### 3. 培训目的位置调整

- 培训目的放在“项目公用信息”中；
- 生成的通知文案里，培训目的出现在开头第二段（称呼之后）。

### 4. 时间字段与截止提醒融合

- 每条通知包含：开始日期、结束日期；
- 截止提醒提供两种模式：
  - **同结束日期**：自动根据结束日期生成“X月X日前”；
  - **自定义**：手动填写截止时间/要求（如“8月28日下班前”）。

### 5. 附件资源动态添加

- 不再固定显示“学习材料/参考文档/作业提交”三个输入框；
- 改为“添加附件”，可选择类型：学习材料、参考文档、作业提交、日程安排、分组信息、其他资料；
- 每项附件包含：类型、显示名称、链接；
- 未填写链接的附件不会出现在通知中。

### 6. 图片支持

- 每条通知支持上传图片；
- 发送时先发送文本，再发送图片；
- 图片会自动上传到企业微信获取 media_id 后发送。

### 7. 文案智能更新

- 未手动修改文案时，字段变更自动重新生成；
- 手动修改过文案后，字段变更会尝试“局部替换”：
  - 日期、地点/链接、截止时间、阶段名称、附件、联系人等会尝试智能同步；
  - 如果系统无法安全替换，会提示“字段已变更，建议检查或重新生成”。
- 点击“重新生成文案”会按最新字段完整生成。

### 8. 通知内容标题

- 去掉了通知顶部的 `【XX通知】` 标题行；
- 通知内容直接以称呼开头。

### 9. 项目间完全独立

- 每个项目的智能初始化输入、通知条目、群配置、负责人都是独立的；
- 切换项目不会互相影响。

## 使用流程

1. **新建项目** 或选择已有项目；
2. 填写 **项目公用信息**：项目名称、周期类型、培训目的；
3. 填写 **培训负责人**；
4. 在 **通知群登记** 中添加群名称、群类别、Webhook；
5. 使用 **智能初始化** 粘贴培训日程，自动识别阶段和时间；
6. 逐条检查/修改通知内容、目标群、附件、图片；
7. 点击 **保存项目**；
8. 到点后服务自动发送，或点击 **立即发送这条 / 立即发送全部** 测试。

## 文件说明

| 文件 | 说明 |
|---|---|
| `training-notification-server.js` | 本地服务，托管页面、保存项目、调用企微接口 |
| `training-notification-demo.html` | 可交互页面 |
| `channels.config.json` | 群通道配置示例（可选，页面内也可直接配置） |
| `projects/*.json` | 自动保存的培训项目文件 |

## 常见问题

### Webhook 返回 invalid webhook url

- 确认 Webhook 来自企业微信内部群的“消息推送/群机器人”；
- 确认机器人仍在群内、未被移除；
- 确认企业未停用消息推送能力；
- 含微信用户的外部客户群可能不支持此方式。

### 图片发送失败

- 企业微信图片要求：2MB 以内，支持 JPG/PNG；
- 确认图片格式正确。

### 定时发送不生效

- 需要保持 `training-notification-server.js` 持续运行；
- 服务每分钟扫描一次，到点触发。
